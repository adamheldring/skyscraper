let axios,cheerio;_4b9‍.x([["getHTML",()=>getHTML],["getTwitterFollowers",()=>getTwitterFollowers],["getInstagramFollowers",()=>getInstagramFollowers]]);_4b9‍.w("axios",[["default",["axios"],function(v){axios=v}]]);_4b9‍.w("cheerio",[["default",["cheerio"],function(v){cheerio=v}]]);


       const getHTML = async url => {
  const { data: html } = await axios.get(url);
  return html;
};

       const getTwitterFollowers = async html => {
  const $ = cheerio.load(html);
  const el = $("a[data-nav='followers'] span[class='ProfileNav-value']");

  console.log("Getting Twitter followers...");
  const followers = el.data("count");
  return followers;
};

       const getInstagramFollowers = async html => {
  const $ = await cheerio.load(html);
  const dataAsString = $("body script").html();
  const beginningOfCountObject = dataAsString.indexOf('{"count"');

  const dataCutBeginning = dataAsString.slice(beginningOfCountObject);
  const dataCutBothEnds = dataCutBeginning.slice(
    0,
    dataCutBeginning.indexOf("}") + 1
  );
  _4b9‍.g.console.log(typeof JSON.parse(dataCutBothEnds));
  _4b9‍.g.console.log(typeof dataCutBothEnds);
  _4b9‍.g.console.log(JSON.parse(dataCutBothEnds).count);

  console.log("Getting Instagram followers...");
  return "TEST VALUE";
};

// const getMonthlySpotifyListeners = html => {
//   const $ = cheerio.load(html);
//   const el = $;
// };
